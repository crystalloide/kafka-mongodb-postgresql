# Fichiers de départ — Élève

Commencer par lire `../ENONCE_TP_CQRS.md`.

Les fichiers de ce dossier sont conçus pour être complétés :

- `command_api.py` : TODO sur la transaction PostgreSQL et l'Outbox ;
- `outbox_publisher.py` : TODO sur la publication Kafka ;
- `projector.py` : TODO sur l'idempotence et `aggregate_version` ;
- `query_api.py` : déjà fonctionnel, à conserver et tester.

Le dossier `socle_initial/` contient la version de départ du TP avant évolution.
